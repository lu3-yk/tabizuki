<div class="container">
  <div class="row mt-4">
    <div class="col">
      <h2 class="text-center"><%= @user.name %>さんのプロフィールを編集</h2>
    </div>
  </div>
  <div class="row mt-3">
    <div class="mx-auto">
      <%= form_with model: @user, url: admin_user_path(@user) do |f| %>
        <!--エラーメッセージ-->
        <%= render 'layouts/error_message', model: f.object %>
        <table class="table table-borderless text-center shadow">
          <tr>
            <th>プロフェール写真</th>
            <td><%= image_tag @user.get_profile_image(50,50), class: "user-icon" %></td>
          </tr>
          <tr>
            <th>名前を変更</th>
            <td><%= f.text_field :name, autofocus: true, id:"inputName", placeholder:"名前"%></td>
          </tr>
          <tr>
            <th>メールアドレスを変更</th>
            <td><%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
          </tr>
        </table>
        <div class="text-center">
          <%= f.submit "変更を保存", class: "btn btn-outline-primary" %>
        </div>
      <% end %>
      <div class="mt-5 text-center">
        <%= link_to "＜会員一覧画面へ戻る", admin_users_path, class: "btn btn-outline-dark btn-sm" %>　&emsp;
        <% if !@user == @user.is_deleted %>
          <%= link_to "退会させる", unsubscribe_admin_user_path(@user), class: "btn btn-outline-danger btn-sm" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
